<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.resumematch">
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ResumeMatch"
        tools:targetApi="31">
        
        <activity android:name=".activities.WelcomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity android:name=".activities.MainActivity" />
        <activity android:name=".activities.JobManagementActivity" />
        <activity android:name=".activities.ProfileActivity" />
        <activity android:name=".activities.CreateJobActivity" />
        <activity android:name=".activities.ScanResumeActivity" />
        <activity android:name=".activities.MatchScoreActivity" />
        <activity android:name=".activities.EmployerHomeActivity" />
        <activity android:name=".activities.ResumeListActivity" />
        <activity android:name=".activities.RecentResumesActivity" />
        <activity android:name=".activities.JobSelectionActivity" />
        <activity android:name=".activities.EditJobActivity" />
        <activity android:name=".activities.ResumeDetailsActivity" />
        <activity android:name=".activities.JobTemplateActivity" />
        <activity android:name=".activities.StoreProfileActivity" />
        <activity android:name=".activities.ResumePhotoViewerActivity" />
        <activity android:name=".activities.ResumeImageActivity" />
        <activity android:name=".activities.JobApplicationsActivity" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.example.resumematch.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
